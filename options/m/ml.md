<!-- TITLE: ml -->

#  `ml` List filesystem plugins


```text
ext2  ext2 filesystem
       fat  FAT filesystem
        fb  FB filesystem
       hfs  HFS filesystem
   hfsplus  HFSPLUS filesystem
        io  r_io based filesystem
   iso9660  ISO9660 filesystem
       jfs  JFS filesystem
     minix  MINIX filesystem
      ntfs  NTFS filesystem
     posix  POSIX filesystem
        r2  r2-based filesystem
  reiserfs  REISERFS filesystem
       sfs  Amiga Smart FileSystem
       tar  TAR filesystem
       udf  UDF filesystem
       ufs  UFS filesystem
       xfs  XFS filesystem
```
